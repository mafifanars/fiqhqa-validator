rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes to all documents.
    // This is insecure and not recommended for production.
    // This is for development purposes only based on the current auth model.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
